# sample_images

This folder is intentionally committed without binary image files to keep PR diffs text-only.

Add your own photos here, or generate a tiny demo set with:

```bash
python scripts/generate_sample_images.py
```

Then run:

```bash
docker compose up --build
```
